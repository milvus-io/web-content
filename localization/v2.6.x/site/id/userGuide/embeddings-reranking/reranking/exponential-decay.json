{"codeList":["from pymilvus import Function, FunctionType\nimport datetime\n\n# Create an exponential decay ranker for news recency\nranker = Function(\n    name=\"news_recency\",                  # Function identifier\n    input_field_names=[\"publish_time\"],   # Numeric field to use\n    function_type=FunctionType.RERANK,    # Function type. Must be RERANK\n    params={\n        \"reranker\": \"decay\",              # Specify decay reranker\n        \"function\": \"exp\",                # Choose exponential decay\n        \"origin\": int(datetime.datetime.now().timestamp()),  # Current time\n        \"offset\": 3 * 60 * 60,            # 3 hour breaking news window\n        \"decay\": 0.5,                     # Half score at scale distance\n        \"scale\": 24 * 60 * 60             # 24 hours (1 day)\n    }\n)\n","# Apply decay ranker to vector search\nresult = milvus_client.search(\n    collection_name,\n    data=[\"market analysis\"],             # Query text\n    anns_field=\"dense\",                   # Vector field to search\n    limit=10,                             # Number of results\n    output_fields=[\"title\", \"publish_time\"], # Fields to return\n    #  highlight-next-line\n    ranker=ranker,                        # Apply the decay ranker\n    consistency_level=\"Bounded\"\n)\n","from pymilvus import AnnSearchRequest\n\n# Define dense vector search request\ndense = AnnSearchRequest(\n    data=[\"market analysis\"],\n    anns_field=\"dense\",\n    param={},\n    limit=10\n)\n\n# Define sparse vector search request\nsparse = AnnSearchRequest(\n    data=[\"market analysis\"],\n    anns_field=\"sparse_vector\",\n    param={},\n    limit=10\n)\n\n# Apply decay ranker to hybrid search\nhybrid_results = milvus_client.hybrid_search(\n    collection_name,\n    [dense, sparse],                      # Multiple search requests\n    #  highlight-next-line\n    ranker=ranker,                        # Same decay ranker\n    limit=10,\n    output_fields=[\"title\", \"publish_time\"]\n)\n"],"headingContent":"Exponential Decay","anchorList":[{"label":"Peluruhan EksponensialCompatible with Milvus 2.6.x","href":"Exponential-Decay","type":1,"isActive":false},{"label":"Kapan menggunakan peluruhan eksponensial","href":"When-to-use-exponential-decay","type":2,"isActive":false},{"label":"Prinsip penurunan yang tajam","href":"Sharp-drop-off-principle","type":2,"isActive":false},{"label":"Rumus","href":"Formula","type":2,"isActive":false},{"label":"Gunakan peluruhan eksponensial","href":"Use-exponential-decay","type":2,"isActive":false}]}